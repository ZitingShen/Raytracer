#ifndef FINISH_H
#define FINISH_H

class Finish { // material without colour
	public:
		int id;
		float ambient, diffuse, specular, shininess;
		float reflectivity, transmission, refraction;
};

#endif
